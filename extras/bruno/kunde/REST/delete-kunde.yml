info:
  name: Kunde loeschen
  type: http
  seq: 3

http:
  method: DELETE
  url: "{{baseUrl}}/rest/1005"
  headers:
    - name: Authorization
      value: Bearer {{accessToken}}

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
