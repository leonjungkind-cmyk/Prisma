info:
  name: GET nur Anzahl
  type: http
  seq: 4

http:
  method: GET
  url: https://localhost:3000/rest?count-only=true
  params:
    - name: count-only
      value: "true"
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
