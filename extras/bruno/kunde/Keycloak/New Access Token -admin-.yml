info:
  name: New Access Token -admin-
  type: http
  seq: 1

http:
  method: POST
  url: "{{baseUrl}}/auth/token"
  headers:
    - name: Content-Type
      value: application/json
  body:
    type: json
    data: |-
      {
        "username": "admin",
        "password": "p"
      }

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
